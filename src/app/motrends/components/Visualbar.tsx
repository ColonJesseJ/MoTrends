'use client';

import { useState } from "react";


export default function VisualBar({ currentStep }: { currentStep: number }) {
    // ui for visualdash
    return (
        <>

        </>
    )
}

